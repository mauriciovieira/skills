name: Release

on:
  # pull_request_target (not pull_request) so GITHUB_TOKEN gets write access
  # and secrets even when the merged PR came from a fork. Safe here: every
  # step below operates on main's own content (checkout ref: main) and never
  # checks out or executes code from the PR branch itself.
  pull_request_target:
    types: [closed]
    branches: [main]

concurrency:
  group: release
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determine bump type
        id: bumptype
        env:
          LABELS: ${{ toJson(github.event.pull_request.labels.*.name) }}
        run: |
          set -euo pipefail

          # Defaults to patch. Apply the bump:minor or bump:major label to a
          # PR before merging to override for that release.
          if echo "$LABELS" | jq -e 'index("bump:major")' > /dev/null; then
            echo "type=major" >> "$GITHUB_OUTPUT"
          elif echo "$LABELS" | jq -e 'index("bump:minor")' > /dev/null; then
            echo "type=minor" >> "$GITHUB_OUTPUT"
          else
            echo "type=patch" >> "$GITHUB_OUTPUT"
          fi

      - name: Bump version
        id: bump
        run: |
          set -euo pipefail

          BUMP_TYPE="${{ steps.bumptype.outputs.type }}"
          CURRENT_VERSION=$(jq -r '.version' .claude-plugin/plugin.json)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"
          case "$BUMP_TYPE" in
            major) NEW_VERSION="$((MAJOR + 1)).0.0" ;;
            minor) NEW_VERSION="${MAJOR}.$((MINOR + 1)).0" ;;
            *) NEW_VERSION="${MAJOR}.${MINOR}.$((PATCH + 1))" ;;
          esac

          jq --indent 2 --arg v "$NEW_VERSION" '.version = $v' .claude-plugin/plugin.json > /tmp/plugin.json
          mv /tmp/plugin.json .claude-plugin/plugin.json

          echo "new_version=$NEW_VERSION" >> "$GITHUB_OUTPUT"

      - name: Build changelog entry
        id: changelog
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          PR_NUMBER="${{ github.event.pull_request.number }}"
          PR_LINK="[#${PR_NUMBER}](https://github.com/${{ github.repository }}/pull/${PR_NUMBER})"

          FEATURES=""
          FIXES=""
          OTHER=""
          SUBJECT=""
          # Trailing !? matches Conventional Commits breaking-change markers
          # (feat!: / feat(scope)!:) so they don't fall through to OTHER.
          TYPE_REGEX='^([a-z]+)(\([^)]*\))?!?:\ (.*)$'

          # Read the PR's own (pre-squash) commit list from the API instead of
          # a git rev-range. A range computed from base.sha is only reliable
          # when the PR branch was rebased right up to the merge - base.sha
          # reflects whatever the base ref was when GitHub last synced it,
          # which can drift stale and pull in another PR's commits (and its
          # own release commit) into this PR's changelog entry. The commits
          # API returns exactly this PR's commits regardless of merge
          # strategy or how far main moved underneath it.
          # select((.parents | length) < 2) drops merge commits (e.g. from
          # merging main into the branch), same as git log's --no-merges did.
          #
          # `|| [ -n "$SUBJECT" ]` is required: the jq output has no trailing
          # newline, and bash's `read` silently drops an unterminated final
          # line otherwise - which is exactly why a single-commit PR (e.g. a
          # squash merge) would otherwise produce an empty changelog entry.
          # SUBJECT is pre-initialized above so the -n test is always well
          # defined under `set -u`, even on a zero-output range.
          while IFS= read -r SUBJECT || [ -n "$SUBJECT" ]; do
            [ -z "$SUBJECT" ] && continue
            if [[ "$SUBJECT" =~ $TYPE_REGEX ]]; then
              TYPE="${BASH_REMATCH[1]}"
              DESC="${BASH_REMATCH[3]}"
            else
              TYPE=""
              DESC="$SUBJECT"
            fi
            # A squash-merge's own commit subject would carry a trailing
            # "(#<PR_NUMBER>)", but these are the PR's pre-squash commits, so
            # this is only a defensive no-op in the common case.
            DESC="${DESC% (#${PR_NUMBER})}"
            case "$TYPE" in
              feat) FEATURES="${FEATURES}- ${DESC} (${PR_LINK})"$'\n' ;;
              fix) FIXES="${FIXES}- ${DESC} (${PR_LINK})"$'\n' ;;
              *) OTHER="${OTHER}- ${DESC} (${PR_LINK})"$'\n' ;;
            esac
          done < <(gh api "repos/${{ github.repository }}/pulls/${PR_NUMBER}/commits" --paginate --jq '.[] | select((.parents | length) < 2) | .commit.message | split("\n")[0]')

          {
            echo "## [${{ steps.bump.outputs.new_version }}] - $(date -u +%Y-%m-%d)"
            echo
            if [ -n "$FEATURES" ]; then
              echo "### Added"
              echo
              printf '%s' "$FEATURES"
              echo
            fi
            if [ -n "$FIXES" ]; then
              echo "### Fixed"
              echo
              printf '%s' "$FIXES"
              echo
            fi
            if [ -n "$OTHER" ]; then
              echo "### Changed"
              echo
              printf '%s' "$OTHER"
              echo
            fi
          } > /tmp/changelog-entry.md

          cat /tmp/changelog-entry.md

      - name: Prepend changelog entry
        run: |
          set -euo pipefail

          if [ ! -f CHANGELOG.md ]; then
            {
              echo "# Changelog"
              echo
              echo "All notable changes to this project are documented in this file."
              echo
            } > CHANGELOG.md
          fi

          {
            head -n 4 CHANGELOG.md
            cat /tmp/changelog-entry.md
            tail -n +5 CHANGELOG.md
          } > /tmp/CHANGELOG.md
          mv /tmp/CHANGELOG.md CHANGELOG.md

      - name: Commit, tag, and push
        run: |
          set -euo pipefail

          NEW_VERSION="${{ steps.bump.outputs.new_version }}"

          git add .claude-plugin/plugin.json CHANGELOG.md
          git commit -m "chore(release): v${NEW_VERSION}"

          # concurrency: group already serializes runs of this workflow, so this
          # guards only against a manual push landing on main mid-job. Tag AFTER
          # the rebase so it points at the commit that actually lands on main.
          git fetch origin main
          git rebase origin/main
          git tag "v${NEW_VERSION}"
          # --atomic: main and the tag land together, or neither does.
          git push --atomic origin main "v${NEW_VERSION}"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          NEW_VERSION="${{ steps.bump.outputs.new_version }}"
          gh release create "v${NEW_VERSION}" \
            --title "v${NEW_VERSION}" \
            --notes-file /tmp/changelog-entry.md
